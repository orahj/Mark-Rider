import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.markrider.app',
  appName: 'Mark Rider',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
