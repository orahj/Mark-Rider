import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.markriders.app',
  appName: 'Mark Rider',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
